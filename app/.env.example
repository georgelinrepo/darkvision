# Copy to .env and fill in after deploying server/template.yaml
# This URL is safe to commit — it's not a secret
EXPO_PUBLIC_NORMALISER_URL=https://<api-id>.execute-api.<region>.amazonaws.com/normalise-move
